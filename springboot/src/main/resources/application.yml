spring:

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/full-stack-ecommerce?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:ecommerceapp}
    password: ${DB_PASSWORD:ecommerceapp}
  data:
    rest:
      #Only DAOs annotated will be exposed.
      detection-strategy: annotated

  jpa:
   
    show-sql: true
    hibernate:
      ddl-auto: none

  application:
    name: ecommerce-webapp

  liquibase:
    change-log: classpath:/db/master-changelog.xml
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${OAUTH_ISSUER:http://localhost:8080/realms/ecommerce}

server:
  port: ${SERVER_PORT:8090}
  servlet:
    context-path: ${APP_CONTEXT_PATH:/api/v1}



env:
  allowed:
    origins: ${ALLOWED_ORIGINS:http://localhost:4200}
#management:
#  endpoints:
#    web:
#      base-path: /actuator
#      exposure:
#        include: [ "health","prometheus", "metrics" ]
#  endpoint:
#    health:
#      show-details: always
#    metrics:
#      enabled: true
#    prometheus:
logging:
  level:
    org:
      springframework:
        web: DEBUG
        security: DEBUG