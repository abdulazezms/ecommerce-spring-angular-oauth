import { BusinessValidators } from './business-validators';

describe('BusinessValidators', () => {
  it('should create an instance', () => {
    expect(new BusinessValidators()).toBeTruthy();
  });
});
